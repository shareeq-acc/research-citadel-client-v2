# Keep the build context small and, more importantly, keep secrets out of it.
# Anything copied into an image is readable by anyone who can pull that image.
.env
.env.*
!.env.example

node_modules
.next
out
build

.git
.gitignore
.vscode
*.md
!README.md

npm-debug.log*
*.log
coverage
