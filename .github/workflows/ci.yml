name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/${{ github.repository_owner }}/research-citadel-client

  # Compiled into the browser bundle at build time, not read at runtime — see
  # the Dockerfile. Changing where the app is hosted means rebuilding, not
  # editing an environment file on the server.
  PUBLIC_API_URL: https://citadel.shareeq.xyz/api/v1

jobs:
  check:
    name: Lint and type-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      # `npm ci` and not `npm install`: it installs exactly the lockfile and
      # fails loudly when the lockfile and package.json disagree, which is the
      # bug this project already had.
      - run: npm ci
      # Type errors block: `next build` fails on them anyway, so catching them
      # here is just catching them sooner and cheaper.
      - run: npx tsc --noEmit
      # Lint is reported but does not block. The codebase has pre-existing
      # `no-explicit-any` violations that predate this pipeline, and gating
      # deploys on them would mean nothing ships until a cleanup nobody has
      # scheduled. Make this a gate once the existing ones are cleared.
      - run: npm run lint
        continue-on-error: true

  build:
    name: Build and publish
    needs: check
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            NEXT_PUBLIC_API_URL=${{ env.PUBLIC_API_URL }}
          # `main` is what the server follows; the sha is what you roll back to.
          tags: |
            ${{ env.IMAGE }}:main
            ${{ env.IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
